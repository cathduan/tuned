1. In one terminal, cd into "backend": cd web-app, then cd backend
2. Type "node server.mjs" into your terminal. This starts up the server.
3. Open up a second terminal.
4. cd into "web-app": cd web-app
5. Type "npm start" into your terminal to start the frontend. 